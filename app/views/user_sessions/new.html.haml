%div.sign_in_form
  %h2 Sign In

  = simple_form_for @user,:html => {:class => "form-horizontal"}, url: user_session_path do |f|
    = f.input :email
    = f.input :password
    %div.text-center
      = f.button :submit, "Sign In"
      


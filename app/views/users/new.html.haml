%div.sign_up_form
  %h2 Sign Up
  = simple_form_for @user,:html => {:class => "form-horizontal"} do |f|
    = f.input :name
    = f.input :email
    = f.input :password
    = f.input :password_confirmation

    %div.text-center
      = f.button :submit, "Go!"



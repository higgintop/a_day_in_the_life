%div.edit-profile-container
  %h1 Edit Profile

  = simple_form_for @user, url: user_path, method: :put do |f|
    = f.input :name
    = f.input :bio
    = f.input :email
    = f.button :submit, "Save Changes", class: 'save-profile-btn'
